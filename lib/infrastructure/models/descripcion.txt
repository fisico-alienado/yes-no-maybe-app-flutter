modelos que nos sirvan para trabajar con los datos que vienen de fuera, por ejemplo, con los json que recibimos de las peticiones http.
Por ejemplo, si algún dia cambian sus keys, etc. Tambien para evitar equivocarnos escribiendo el código, puesto que los strings
de las keys solo se usan en estos archivos de modelos y asi no tenemos que andar poniendo strings en los archivos donde se usen
las llamadas http.